services:
  hauss-server:
    build: .
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - hauss-data:/app/data
      - hauss-uploads:/app/uploads

volumes:
  hauss-data:
  hauss-uploads:
